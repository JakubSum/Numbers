# Numeral_system_converter
Project in Kotlin that let's the user input numbers in different numeral systems and outputs the same value in other system.

The most rescent code is contained in branch "master".
